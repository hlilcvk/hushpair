{
  "name": "hushpair-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:backend": "cd backend && npm install",
    "build:backend": "cd backend && npm run build",
    "start": "cd backend && npm run start:prod",
    "deploy": "npm run install:backend && npm run build:backend"
  }
}
